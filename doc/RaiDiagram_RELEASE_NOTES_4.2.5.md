# RaiDiagram 4.2.5

Coordinated dependency release for accepted CR017.

- Aligns fallback dependencies on OsLibCore, RaiUtils, and RaiImage 4.2.5.
- Keeps `.raid`, PlantUML, style, rendering, and SVG provenance behavior
  unchanged.

Release verification: 34 RaiDiagram Release tests passed through the real
PlantUML integration path.

RAI retains the manual coordinated release gate; no tag or publication is
authorized by these notes.
